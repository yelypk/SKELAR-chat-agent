# Project Assumptions

- Dialogue logic is deterministic and code-driven
- LLMs generate language, not control flow
- One dialogue = one isolated session
- Manual knowledge is provided via intent cards
- Synthetic data must be class-balanced